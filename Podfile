# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GeniusPlazaTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RxSwift'
  pod 'Kingfisher', '~> 4.0'
  
  # Pods for GeniusPlazaTest

  target 'GeniusPlazaTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GeniusPlazaTestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
